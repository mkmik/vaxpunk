[package]
name = "ods-image"
description = "Files-11 volumes in disk image files: the host side of ods-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ods-core = { path = "../ods-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
