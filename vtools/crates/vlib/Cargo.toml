[package]
name = "vlib"
description = "Puts vaxpunk object modules into object libraries, like LIBRARY/OBJECT"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vms-obj = { path = "../vms-obj" }

[dev-dependencies]
vasm = { path = "../vasm" }
